# Claude Config Redirect

- The canonical Claude configuration for this repository lives in `claude-starter/CLAUDE.md`.
- Use `claude-starter/planning/protocol.md` for planning workflow states, checkpoints, artifacts, TDD policy, and handoff rules.
- Use `claude-starter/agents/master-planner.md` when the user asks for planning or uses the `planning:` prefix.
