# Codex Config Redirect

- The canonical Codex configuration for this repository lives in `Codex-starter/AGENTS.md`.
- Use `Codex-starter/planning/protocol.md` for planning workflow states, checkpoints, artifacts, TDD policy, and handoff rules.
- Use `Codex-starter/agents/master-planner.md` when the user asks for planning or uses the `planning:` prefix.
